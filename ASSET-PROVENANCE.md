# Asset provenance

- `主题资源/background.jpg`：为本主题生成的原创 AI 辅助工业科幻背景，未使用官方 CG 帧、角色素材或 Logo。
- `.runtime-macos/presets/preset-endfield-frontier/background.jpg`：macOS 运行副本。
- `.runtime-windows/assets/dream-reference.jpg`：Windows 运行副本。
- `docs/images/preview-desktop.png`：使用当前主题 CSS 与响应式布局生成的 `1920 × 1080` 脱敏预览。
- `docs/images/preview-compact.png`：使用当前主题 CSS 与响应式布局生成的 `480 × 600` 脱敏预览。

预览中的 `nacre-orbit`、`quartz-relay` 与 `vector-harbor` 均为虚构占位项目名，不对应用户仓库。
